name: anal
description: A pluggable static analysis framework for Dart and Flutter projects with an extensible runner, rule registry, and CLI.
version: 0.3.5
homepage: https://github.com/lezli01/anal
repository: https://github.com/lezli01/anal
issue_tracker: https://github.com/lezli01/anal/issues
topics:
  - analyzer
  - cli
  - flutter
  - lint
  - static-analysis

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

executables:
  anal: anal

dependencies:
  flutter:
    sdk: flutter
  analyzer: ">=9.0.0 <13.0.0"
  path: ^1.9.0
  glob: ^2.1.2
  args: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
