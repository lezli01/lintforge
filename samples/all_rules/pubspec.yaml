name: all_rules_sample
description: >-
  Sample project that exercises every built-in rule shipped by the `lintforge`
  static-analysis package in a single tree. Not published to pub.dev.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.12.0

dev_dependencies:
  lintforge:
    path: ../..
