name: all_rules_sample
description: >-
  Sample project that exercises every built-in rule shipped by the `anal`
  static-analysis package in a single tree. Not published to pub.dev.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  anal:
    path: ../..
