// Imported by `lib/unused_source_file_sample.dart` (an entry point) and
// owns `used_via_part.dart` as a `part`. Both files must be reachable and
// must NOT be flagged by the `unused_source_file` rule.
part 'used_via_part.dart';

/// Returns a greeting using the prefix declared in the part file.
String greeting(String name) => '$_prefix $name';
