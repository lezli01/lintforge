// A part of `used.dart`. Reachable transitively via the entry point ->
// `used.dart` -> `used_via_part.dart` chain, so the `unused_source_file`
// rule must NOT flag this file.
part of 'used.dart';

const String _prefix = 'Hello,';
