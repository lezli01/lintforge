name: unused_source_file_sample
description: >-
  Sample project that exercises the `unused_source_file` multi-file rule from
  the `lintforge` static-analysis package. Not published to pub.dev.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.12.0

dev_dependencies:
  lintforge:
    path: ../..
